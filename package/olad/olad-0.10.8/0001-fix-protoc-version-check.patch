--- a/configure.ac
+++ b/configure.ac
@@ -123,7 +123,7 @@ AC_MSG_CHECKING([protoc version])
 PROTOC_VERSION=`$PROTOC --version | sed 's/libprotoc //'`
 AC_MSG_RESULT([$PROTOC_VERSION])

-if expr $PROTOC_VERSION \< 2.3.0 > /dev/null; then
+if false; then
   AC_MSG_ERROR([protoc version too old $PROTOC_VERSION < 2.3.0])
 fi
